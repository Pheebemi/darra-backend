# Django settings
DEBUG=True
SECRET_KEY=your_secret_key_here

# Database
DATABASE_URL=sqlite:///db.sqlite3

# Email settings
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password
DEFAULT_FROM_EMAIL=your_email@gmail.com

# Payment settings
PAYSTACK_SECRET_KEY=sk_test_your_test_secret_key_here
PAYSTACK_PUBLIC_KEY=pk_test_your_test_public_key_here

FLUTTERWAVE_SECRET_KEY=FLWSECK_TEST_your_test_secret_key_here
FLUTTERWAVE_PUBLIC_KEY=FLWPUBK_TEST_your_test_public_key_here
FLUTTERWAVE_ENCRYPTION_KEY=FLWSECK_TEST_your_test_encryption_key_here

# REQUIRED for Flutterwave webhooks. Set the same value here and in the
# "Secret hash" box on the Flutterwave dashboard webhook settings page.
# Flutterwave sends it back in the verif-hash header; without it every
# Flutterwave webhook is rejected, because there is no way to tell a real
# callback from a forged one.
FLUTTERWAVE_SECRET_HASH=pick-a-long-random-string

# Payment provider (paystack or flutterwave)
PAYMENT_PROVIDER=paystack

# Base URL of the API (Django itself) — also used to build admin links
BASE_URL=http://localhost:8000

# Public address of the FRONTEND. Used for links inside emails (receipts,
# sale alerts, ticket emails). Must be set in production or buyers get links
# pointing at the wrong site.
FRONTEND_URL=http://localhost:3000

# Optional. Who gets admin alerts (e.g. a seller requested a payout).
# Leave blank and alerts go to every active superuser in the database, which
# means adding/removing an admin needs no config change. Comma-separated.
ADMIN_ALERT_EMAILS=

# Cloudflare R2 for paid product files (ebooks, audio). Set all four to store
# them in R2 instead of on the server disk; leave any blank to keep local disk.
# Everything else (covers, banners, QR codes) always stays local.
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_ENDPOINT_URL=

# Shared secret that lets the backend trust the real visitor IP forwarded by
# the Next.js frontend proxy, so rate limits apply per visitor instead of to
# the proxy's single IP. Use a long random value and set the SAME value in the
# frontend's PROXY_SHARED_SECRET (Vercel env). Leave blank to disable.
PROXY_SHARED_SECRET=

# Cloudinary settings
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
