# Django and core dependencies
Django>=5.2.4
djangorestframework>=3.14.0
djangorestframework-simplejwt>=5.3.0
# Pinned above the JWT-library CVEs (PYSEC-2026-175..179 etc.) — this is what
# signs/verifies your auth tokens, so keep it patched.
PyJWT>=2.13.0

# CORS handling
django-cors-headers>=4.3.1

# Environment variables
python-dotenv>=1.2.2
dj-database-url>=2.1.0

# Transactional email via Resend's HTTPS API (works on PythonAnywhere free,
# where outbound SMTP is blocked but api.resend.com is allowlisted). Only used
# when RESEND_API_KEY is set; otherwise SMTP/console backends are used.
django-anymail>=10.0

# HTTP requests (for Paystack API integration)
requests>=2.34.2

# Additional utilities
Pillow>=10.0.0  # For image handling
img2pdf>=0.5.1  # For converting images to PDF
qrcode>=7.4.2  # For generating QR codes

# Cloudinary integration (commented out for local storage)
# cloudinary>=1.40.0
# django-cloudinary-storage>=0.3.0

# Cloudflare R2 (S3-compatible) for paid product files. Only used when the
# R2_* settings are configured; otherwise files stay on local disk.
django-storages>=1.14.2
boto3>=1.34.0

# Production server
gunicorn>=21.2.0

# Static files serving
whitenoise>=6.6.0

# Content Security Policy
django-csp>=3.7

# Database drivers and connection pooling
PyMySQL>=1.1.0  # MySQL driver
psycopg2-binary>=2.9.7  # PostgreSQL driver (for production)

# Caching and performance
redis>=4.6.0  # Redis client
django-redis>=5.4.0  # Django Redis integration

# Additional performance dependencies
psutil>=5.9.0  # System monitoring

# Background task processing
celery>=5.3.0  # Background task queue
django-celery-beat>=2.5.0  # Periodic tasks
django-celery-results>=2.5.0  # Task results backend

